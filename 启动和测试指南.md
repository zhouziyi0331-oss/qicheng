# 🚀 启动和测试指南 - 快速开始

## 📋 前置条件

### 需要安装的服务
- ✅ MongoDB（已安装）
- ⏳ Redis（需要安装）
- ✅ Node.js（已安装）

---

## 🔧 第一步：安装和启动服务

### 1. 安装Redis（如果还没有）

**macOS**:
```bash
# 使用Homebrew安装
brew install redis

# 或者使用MacPorts
sudo port install redis
```

**Linux**:
```bash
# Ubuntu/Debian
sudo apt-get install redis-server

# CentOS/RHEL
sudo yum install redis
```

**Windows**:
下载并安装 Redis for Windows

### 2. 启动MongoDB

**macOS**:
```bash
# 使用Homebrew启动
brew services start mongodb-community

# 或者手动启动
mongod --config /usr/local/etc/mongod.conf
```

**Linux**:
```bash
sudo systemctl start mongod
```

**验证MongoDB运行**:
```bash
mongosh
# 应该能够连接到MongoDB
```

### 3. 启动Redis

**macOS**:
```bash
# 使用Homebrew启动
brew services start redis

# 或者手动启动
redis-server
```

**Linux**:
```bash
sudo systemctl start redis
```

**验证Redis运行**:
```bash
redis-cli ping
# 应该返回：PONG
```

---

## 📦 第二步：初始化项目

### 1. 安装后端依赖

```bash
cd /Users/alwan/code/qicheng/miniapp/backend

# 安装Redis客户端（新增依赖）
npm install ioredis

# 检查其他依赖
npm install
```

### 2. 初始化晋级验证数据库

```bash
cd /Users/alwan/code/qicheng

# 添加执行权限
chmod +x init-levelup-database.sh

# 执行初始化脚本
./init-levelup-database.sh
```

**预期输出**:
```
========================================
1. 创建新集合
========================================
✓ levelUpAnswers 集合已创建
✓ levelUpHistory 集合已创建
...
✅ 数据库初始化完成！
```

### 3. 配置环境变量

创建或更新 `backend/.env`:

```bash
cd /Users/alwan/code/qicheng/miniapp/backend

# 创建.env文件
cat > .env << 'EOF'
# MongoDB
MONGO_URI=mongodb://localhost:27017/qicheng

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=your-secret-key-change-in-production

# 微信小程序（开发环境可以留空）
WECHAT_APP_ID=
WECHAT_APP_SECRET=

# 环境
NODE_ENV=development

# 服务器端口
PORT=3000
EOF
```

---

## 🚀 第三步：启动后端服务

```bash
cd /Users/alwan/code/qicheng/miniapp/backend

# 启动开发服务器
npm run dev
```

**预期输出**:
```
✓ MongoDB连接成功
✓ Redis连接成功
✓ 服务器运行在 http://localhost:3000
```

**如果遇到错误**:
- MongoDB未运行 → 检查MongoDB服务状态
- Redis未运行 → 检查Redis服务状态
- 端口占用 → 修改.env中的PORT

---

## 🧪 第四步：测试API

### 测试1：健康检查

```bash
curl http://localhost:3000/health
```

**预期响应**:
```json
{
  "status": "ok",
  "timestamp": "2026-07-24T..."
}
```

### 测试2：发送验证码

```bash
curl -X POST http://localhost:3000/api/auth/send-code \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "13800138000",
    "type": "register"
  }'
```

**预期响应**:
```json
{
  "success": true,
  "message": "验证码已发送"
}
```

**后端控制台应该显示**:
```
📱 [开发环境] 手机号 13800138000 的验证码: 123456
```

### 测试3：手机号注册

```bash
curl -X POST http://localhost:3000/api/auth/register-phone \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "13800138000",
    "code": "123456",
    "accountType": "student",
    "nickname": "测试学生"
  }'
```

**预期响应**:
```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": "...",
    "phone": "13800138000",
    "nickname": "测试学生",
    "accountType": "student",
    "needsOnboarding": true
  }
}
```

### 测试4：检查晋级条件

```bash
# 首先需要获取token（使用上一步的token）
TOKEN="your_token_here"

curl -X POST http://localhost:3000/api/level-up/check \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN"
```

### 测试5：获取晋级对话

```bash
curl -X POST http://localhost:3000/api/level-up/dialog \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "fromLevel": 1,
    "toLevel": 2
  }'
```

---

## 📱 第五步：测试小程序

### 1. 编译小程序

```bash
cd /Users/alwan/code/qicheng/miniapp

# 编译为微信小程序
npm run build:weapp
```

**预期输出**:
```
✔ Webpack
  Compiled successfully in 9.08s
```

### 2. 在微信开发者工具中打开

1. 打开微信开发者工具
2. 导入项目
3. 选择目录：`/Users/alwan/code/qicheng/miniapp`
4. AppID：使用测试号或你的正式AppID

### 3. 测试页面

#### 测试晋级完成页
```
1. 在开发者工具中点击"编译"旁边的"普通编译"下拉
2. 选择"添加编译模式"
3. 页面路径：pages/level-up-done/index
4. 启动参数：level=2&levelTitle=践行者
5. 点击"确定"并编译
6. 应该看到完整的晋级动画
```

#### 测试登录页面
```
1. 访问：packageAuth/pages/login/index
2. 输入手机号：13800138000
3. 点击"获取验证码"
4. 输入验证码：123456
5. 点击"登录"
6. 观察是否跳转
```

---

## 🔍 第六步：检查和调试

### 检查服务状态

```bash
# 检查MongoDB
mongosh --eval "db.adminCommand('ping')"

# 检查Redis
redis-cli ping

# 检查后端
curl http://localhost:3000/health

# 查看后端日志
cd /Users/alwan/code/qicheng/miniapp/backend
# 日志应该在终端显示
```

### 检查数据库

```bash
# 连接MongoDB
mongosh qicheng

# 查看集合
show collections

# 查看学生数据
db.students.findOne()

# 查看晋级历史
db.levelUpHistory.find()

# 退出
exit
```

### 检查Redis

```bash
# 连接Redis
redis-cli

# 查看所有key
keys *

# 查看某个验证码（如果刚发送过）
get "sms:code:13800138000:register"

# 退出
exit
```

---

## ⚠️ 常见问题

### 问题1：MongoDB连接失败

**错误信息**: `MongoNetworkError: connect ECONNREFUSED`

**解决方案**:
```bash
# 检查MongoDB是否运行
mongosh

# 如果连不上，启动MongoDB
brew services start mongodb-community
# 或
mongod --config /usr/local/etc/mongod.conf
```

### 问题2：Redis连接失败

**错误信息**: `Error: Redis connection to localhost:6379 failed`

**解决方案**:
```bash
# 检查Redis是否运行
redis-cli ping

# 如果连不上，启动Redis
brew services start redis
# 或
redis-server
```

### 问题3：端口被占用

**错误信息**: `Error: listen EADDRINUSE: address already in use :::3000`

**解决方案**:
```bash
# 查找占用端口的进程
lsof -i :3000

# 杀死进程
kill -9 <PID>

# 或者修改.env中的PORT
```

### 问题4：npm install失败

**错误信息**: `npm ERR! code EACCES`

**解决方案**:
```bash
# 清理npm缓存
npm cache clean --force

# 删除node_modules重新安装
rm -rf node_modules package-lock.json
npm install
```

### 问题5：小程序编译失败

**错误信息**: 各种编译错误

**解决方案**:
```bash
# 清理编译缓存
rm -rf dist node_modules

# 重新安装依赖
npm install

# 重新编译
npm run build:weapp
```

---

## 📊 验证清单

完成以下检查，确保系统正常：

- [ ] MongoDB正在运行
- [ ] Redis正在运行
- [ ] 后端服务启动成功
- [ ] 可以发送验证码
- [ ] 可以注册账号
- [ ] 可以登录
- [ ] 小程序编译成功
- [ ] 可以在开发者工具中预览
- [ ] 晋级完成页动画正常
- [ ] 登录页面可以访问

---

## 🎯 下一步

完成上述测试后，您可以：

1. **集成前端页面** - 按照 `注册登录系统-前端集成指南.md`
2. **添加测试数据** - 创建一些测试用户和任务
3. **测试完整流程** - 从注册到晋级的完整流程
4. **准备上线** - 配置生产环境

---

## 📞 快速命令参考

```bash
# 一键启动所有服务（macOS）
brew services start mongodb-community
brew services start redis

# 启动后端
cd /Users/alwan/code/qicheng/miniapp/backend && npm run dev

# 编译小程序
cd /Users/alwan/code/qicheng/miniapp && npm run build:weapp

# 测试API
curl -X POST http://localhost:3000/api/auth/send-code \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138000","type":"register"}'

# 开发环境验证码
123456
```

---

**准备好开始了吗？按照这个指南一步步来！** 🚀

**有问题随时查看这个文档！** 📖
