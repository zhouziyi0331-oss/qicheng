# 📝 注册登录系统 - 前端集成指南

## 🎯 当前状态

**后端**: ✅ 100%完成  
**前端**: ⏳ 需要更新现有页面  
**现有页面**: ✅ 已存在基础页面  

---

## 📂 现有文件

### 已存在的页面
```
miniapp/src/packageAuth/pages/
├── login/
│   ├── index.tsx       ✅ 已存在（需要更新）
│   └── index.scss      ✅ 已存在
├── register/
│   ├── index.tsx       ✅ 已存在（需要更新）
│   └── index.scss      ✅ 已存在
└── bind-phone/
    ├── index.tsx       ✅ 已存在（需要更新）
    └── index.scss      ✅ 已存在
```

### 新创建的文件
```
miniapp/src/services/
└── authAPI.ts          ✅ 新建（已完成）
```

---

## 🔧 集成步骤

### 步骤1：更新登录页面

**文件**: `src/packageAuth/pages/login/index.tsx`

**需要修改的部分**:

#### 1.1 导入新的API
```typescript
// 替换旧的导入
// import { authAPI } from '../../../services/api'

// 使用新的authAPI
import { authAPI } from '../../../services/authAPI'
```

#### 1.2 更新手机号登录逻辑
```typescript
// 发送验证码
const handleSendCode = async () => {
  if (!phone) {
    Taro.showToast({ title: '请输入手机号', icon: 'none' })
    return
  }

  try {
    Taro.showLoading({ title: '发送中...' })
    
    // 使用新API
    await authAPI.sendCode(phone, 'login')
    
    Taro.hideLoading()
    Taro.showToast({ title: '验证码已发送', icon: 'success' })
    
    // 开发环境提示
    if (process.env.NODE_ENV === 'development') {
      setTimeout(() => {
        Taro.showModal({
          title: '开发环境提示',
          content: '验证码：123456',
          showCancel: false
        })
      }, 1000)
    }
    
    // 开始倒计时
    startCountdown()
    
  } catch (error: any) {
    Taro.hideLoading()
    Taro.showToast({ title: error.message || '发送失败', icon: 'none' })
  }
}

// 手机号登录
const handlePhoneLogin = async () => {
  if (!phone || !code) {
    Taro.showToast({ title: '请输入手机号和验证码', icon: 'none' })
    return
  }

  try {
    setLoading(true)
    Taro.showLoading({ title: '登录中...' })

    // 使用新API
    const result = await authAPI.loginByPhone(phone, code)

    Taro.hideLoading()
    Taro.showToast({ title: '登录成功', icon: 'success' })

    // 根据是否需要完成测评跳转
    setTimeout(() => {
      if (result.user.needsOnboarding) {
        // 需要完成OPC测评
        Taro.redirectTo({ url: '/packageOnboarding/pages/opc-test/index' })
      } else {
        // 直接进入主页
        Taro.switchTab({ url: '/pages/index/index' })
      }
    }, 1500)

  } catch (error: any) {
    setLoading(false)
    Taro.hideLoading()
    Taro.showToast({ title: error.message || '登录失败', icon: 'none' })
  }
}
```

#### 1.3 更新微信登录逻辑
```typescript
const handleWechatLogin = async () => {
  try {
    Taro.showLoading({ title: '登录中...' })

    // 获取微信登录code
    const loginRes = await Taro.login()

    if (!loginRes.code) {
      throw new Error('获取微信登录凭证失败')
    }

    // 使用新API
    const result = await authAPI.wechatLogin(loginRes.code, accountType || 'student')

    Taro.hideLoading()

    if (result.needsBindPhone) {
      // 需要绑定手机号
      Taro.navigateTo({
        url: `/packageAuth/pages/bind-phone/index?openid=${result.openid}&accountType=${accountType}`
      })
    } else {
      // 登录成功
      Taro.showToast({ title: '登录成功', icon: 'success' })

      setTimeout(() => {
        if (result.user?.needsOnboarding) {
          Taro.redirectTo({ url: '/packageOnboarding/pages/opc-test/index' })
        } else {
          Taro.switchTab({ url: '/pages/index/index' })
        }
      }, 1500)
    }

  } catch (error: any) {
    Taro.hideLoading()
    Taro.showToast({ title: error.message || '微信登录失败', icon: 'none' })
  }
}
```

---

### 步骤2：更新注册页面

**文件**: `src/packageAuth/pages/register/index.tsx`

**需要添加的功能**:

#### 2.1 账号类型选择
```typescript
const [accountType, setAccountType] = useState<'student' | 'enterprise'>('student')

// UI组件
<View className="account-type-selector">
  <View className="type-label">选择账号类型</View>
  <View className="type-options">
    <View
      className={`type-option ${accountType === 'student' ? 'active' : ''}`}
      onClick={() => setAccountType('student')}
    >
      <Text className="option-icon">👨‍🎓</Text>
      <Text className="option-text">学生</Text>
    </View>
    <View
      className={`type-option ${accountType === 'enterprise' ? 'active' : ''}`}
      onClick={() => setAccountType('enterprise')}
    >
      <Text className="option-icon">🏢</Text>
      <Text className="option-text">企业</Text>
    </View>
  </View>
</View>
```

#### 2.2 注册逻辑
```typescript
const handleRegister = async () => {
  if (!phone || !code) {
    Taro.showToast({ title: '请输入手机号和验证码', icon: 'none' })
    return
  }

  try {
    setLoading(true)
    Taro.showLoading({ title: '注册中...' })

    // 使用新API
    const result = await authAPI.registerByPhone(
      phone,
      code,
      accountType,
      nickname
    )

    Taro.hideLoading()
    Taro.showToast({ title: '注册成功', icon: 'success' })

    // 跳转到OPC测评（新用户必须完成）
    setTimeout(() => {
      Taro.redirectTo({ url: '/packageOnboarding/pages/opc-test/index' })
    }, 1500)

  } catch (error: any) {
    setLoading(false)
    Taro.hideLoading()
    Taro.showToast({ title: error.message || '注册失败', icon: 'none' })
  }
}

// 发送注册验证码
const handleSendCode = async () => {
  if (!phone) {
    Taro.showToast({ title: '请输入手机号', icon: 'none' })
    return
  }

  try {
    Taro.showLoading({ title: '发送中...' })
    
    // 使用新API - 注意type为register
    await authAPI.sendCode(phone, 'register')
    
    Taro.hideLoading()
    Taro.showToast({ title: '验证码已发送', icon: 'success' })
    
    // 开发环境提示
    if (process.env.NODE_ENV === 'development') {
      setTimeout(() => {
        Taro.showModal({
          title: '开发环境提示',
          content: '验证码：123456',
          showCancel: false
        })
      }, 1000)
    }
    
    startCountdown()
    
  } catch (error: any) {
    Taro.hideLoading()
    Taro.showToast({ title: error.message || '发送失败', icon: 'none' })
  }
}
```

---

### 步骤3：更新绑定手机页面

**文件**: `src/packageAuth/pages/bind-phone/index.tsx`

**需要修改的逻辑**:

```typescript
const handleBindPhone = async () => {
  if (!phone || !code) {
    Taro.showToast({ title: '请输入手机号和验证码', icon: 'none' })
    return
  }

  try {
    setLoading(true)
    Taro.showLoading({ title: '绑定中...' })

    // 从URL参数获取openid和accountType
    const params = Taro.getCurrentInstance().router?.params
    const openid = params?.openid
    const accountType = params?.accountType || 'student'

    // 使用新API
    const result = await authAPI.bindPhone(
      phone,
      code,
      openid,
      accountType as 'student' | 'enterprise'
    )

    Taro.hideLoading()
    Taro.showToast({ title: '绑定成功', icon: 'success' })

    // 跳转到OPC测评
    setTimeout(() => {
      Taro.redirectTo({ url: '/packageOnboarding/pages/opc-test/index' })
    }, 1500)

  } catch (error: any) {
    setLoading(false)
    Taro.hideLoading()
    Taro.showToast({ title: error.message || '绑定失败', icon: 'none' })
  }
}
```

---

### 步骤4：更新OPC测评完成逻辑

**文件**: `src/packageOnboarding/pages/opc-test/result/index.tsx` (或类似的结果页)

**测评完成后的处理**:

```typescript
const handleTestComplete = async () => {
  try {
    // 1. 保存测评结果到后端
    await saveOPCResult(testResult)

    // 2. 更新用户的hasCompletedOnboarding状态
    const token = Taro.getStorageSync('token')
    await Taro.request({
      url: 'http://localhost:3000/api/auth/profile',
      method: 'PUT',
      header: {
        'Authorization': `Bearer ${token}`
      },
      data: {
        hasCompletedOnboarding: true,
        opcCompleted: true,
        opcCompletedAt: new Date()
      }
    })

    // 3. 跳转到主页
    Taro.showToast({ title: '测评完成', icon: 'success' })
    setTimeout(() => {
      Taro.switchTab({ url: '/pages/index/index' })
    }, 1500)

  } catch (error) {
    console.error('保存测评结果失败', error)
    Taro.showToast({ title: '保存失败', icon: 'none' })
  }
}
```

---

## 🧪 测试流程

### 1. 手机号注册流程
```
1. 访问注册页面
2. 选择账号类型（学生/企业）
3. 输入手机号：13800138000
4. 点击"获取验证码"
5. 输入验证码：123456（开发环境）
6. 点击"注册"
7. 应该跳转到OPC测评页面
```

### 2. 手机号登录流程
```
1. 访问登录页面
2. 输入手机号：13800138000
3. 点击"获取验证码"
4. 输入验证码：123456
5. 点击"登录"
6. 如果已完成测评 → 跳转主页
7. 如果未完成测评 → 跳转测评页面
```

### 3. 微信登录流程
```
1. 访问登录页面
2. 点击"微信一键登录"
3. 如果首次登录 → 跳转到绑定手机页面
4. 输入手机号和验证码
5. 点击"绑定"
6. 跳转到OPC测评页面
```

---

## ⚠️ 注意事项

### 1. 环境配置
确保后端的BASE_URL正确：
```typescript
// src/services/authAPI.ts
const BASE_URL = 'http://localhost:3000'  // 开发环境
// const BASE_URL = 'https://api.yourdomain.com'  // 生产环境
```

### 2. 开发环境验证码
- 开发环境固定验证码：`123456`
- 生产环境需要配置真实短信服务

### 3. Redis依赖
后端需要Redis运行：
```bash
brew services start redis
```

### 4. Token存储
新的authAPI会自动处理token存储：
```typescript
Taro.setStorageSync('token', res.data.token)
Taro.setStorageSync('userInfo', res.data.user)
```

### 5. OPC测评强制流程
- 所有新注册用户 `needsOnboarding: true`
- 必须完成测评才能进入主页
- 测评完成后更新 `hasCompletedOnboarding: true`

---

## 🎯 快速集成方案

如果您希望快速测试，可以：

### 方案1：最小改动（推荐）
只修改登录页面的关键逻辑：
1. 导入新的authAPI
2. 更新handleWechatLogin函数
3. 更新handlePhoneLogin函数
4. 测试基本流程

### 方案2：完整实现
按照上面的步骤逐个更新：
1. 更新登录页面
2. 更新注册页面
3. 更新绑定手机页面
4. 更新OPC测评流程
5. 全面测试

---

## 📚 相关文档

- **后端API文档**: `注册登录系统-后端完成总结.md`
- **实施计划**: `注册登录系统-实施计划.md`
- **API服务**: `src/services/authAPI.ts`

---

## 🚀 下一步

1. **今天**: 按照本文档更新登录页面
2. **明天**: 更新注册页面和绑定手机页面
3. **后天**: 完善OPC测评流程，全面测试

---

**准备好开始集成了吗？从哪个页面开始？** 🎯
