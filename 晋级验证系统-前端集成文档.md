# 晋级验证系统 - 前端集成文档

## 📋 概述

这是一个基于学生真实数据的"导师对话式"晋级验证系统。每次晋级时，导师会根据学生的实际表现（任务完成情况、问卷数据、对话记录等）生成个性化的对话内容。

---

## 🎯 核心特点

1. **个性化问题**：基于学生真实数据动态生成
2. **导师口吻**：像陪伴者在回顾成长，不是系统在考试
3. **单一问题**：每级只问一个核心问题
4. **真诚选项**：包含"不知道"/"没感觉"选项
5. **数据展示**：通过数据卡片展示学生的成长轨迹

---

## 📱 前端页面结构

### 页面1：成长主页（GrowthHome）
**路径**：`pages/growth/index`

**功能**：
- 显示当前等级和进度
- 显示晋级通知卡（满足条件时）

**通知卡触发逻辑**：
```javascript
// 完成任务后调用
const checkResult = await request({
  url: '/api/level-up/check',
  method: 'POST',
  data: { completedOrderId: 'xxx' }
})

if (checkResult.shouldLevelUp) {
  // 显示通知卡
  showNotificationCard(checkResult.fromLevel, checkResult.toLevel)
}
```

### 页面2：晋级弹窗（LevelUpSheet）
**组件路径**：`components/level-up-sheet/index`

**结构**：
```
弹窗 (Bottom Sheet)
├── 顶部拖拽条
├── 导师信息栏
│   ├── 导师头像
│   ├── 导师名称 + 角色
│   └── 关闭按钮（选择后显示）
├── 对话区域（可滚动）
│   ├── 导师讲述（多段文字）
│   ├── 数据卡片（可选）
│   │   └── 多行数据展示
│   ├── 时间线（仅Lv.4→5）
│   └── 导师回复（选择后显示）
├── 问题区域（固定）
│   ├── 问题标签
│   ├── 问题文本
│   └── 4个选项按钮
└── 底部操作区
    └── 晋级按钮（选择后显示）
```

### 页面3：晋级完成页（LevelUpDone）
**路径**：`pages/level-up-done/index`

**特点**：
- 全屏黑色背景
- 仪式感动效（光晕、粒子）
- 显示新等级和解锁权益

---

## 🔌 后端接口

### 1. 检查晋级条件
```
POST /api/level-up/check
```

**请求参数**：
```json
{
  "completedOrderId": "order_xxx"  // 刚完成的任务ID（可选）
}
```

**返回数据**：
```json
{
  "success": true,
  "data": {
    "shouldLevelUp": true,           // 是否满足晋级条件
    "fromLevel": 1,                  // 当前等级
    "toLevel": 2,                    // 目标等级
    "completedCount": 3,             // 已完成任务数
    "requiredCount": 3               // 所需任务数
  }
}
```

### 2. 获取晋级对话
```
POST /api/level-up/dialog
```

**请求参数**：
```json
{
  "fromLevel": 1,
  "toLevel": 2
}
```

**返回数据**：
```json
{
  "success": true,
  "data": {
    "mentorText": [
      "三单了。我翻了一下你第一单和这一单，有件事你应该知道。",
      "我不确定你自己有没有注意到这个变化——"
    ],
    "question": "你觉得，是什么不一样了？",
    "options": [
      { "letter": "A", "text": "工具用熟了，不用想就知道点哪里" },
      { "letter": "B", "text": "接到任务大概知道先做什么了" },
      { "letter": "C", "text": "不那么怕做错了" },
      { "letter": "D", "text": "我其实没觉得有什么不一样，就是做完了" }
    ],
    "mentorReply": "行。不管是哪种，三单都做完了。\n\n走，Lv.2。",
    "dataCards": [{
      "rows": [
        { "label": "第1单 · 修改次数", "value": "3 次", "highlight": false },
        { "label": "第3单 · 修改次数", "value": "0 次（直接过）", "highlight": true },
        { "label": "第1单 · 问导师次数", "value": "5 次", "highlight": false },
        { "label": "第3单 · 问导师次数", "value": "0 次（自己做完了）", "highlight": true }
      ]
    }]
  }
}
```

**特殊字段说明**：
- `dataCards`：数据卡片数组，每个卡片包含多行数据
- `timeline`：时间线数组（仅Lv.4→5有），每个节点包含日期和描述
- `highlight`：true为绿色高亮，false为灰色

### 3. 提交答案
```
POST /api/level-up/answer
```

**请求参数**：
```json
{
  "fromLevel": 1,
  "toLevel": 2,
  "selectedOption": "A"  // A/B/C/D
}
```

**返回数据**：
```json
{
  "success": true,
  "data": {
    "message": "答案已记录"
  }
}
```

**注意**：答案仅记录，不影响晋级。选择任意选项都会触发导师回复和晋级按钮显示。

### 4. 确认晋级
```
POST /api/level-up/confirm
```

**请求参数**：
```json
{
  "toLevel": 2
}
```

**返回数据**：
```json
{
  "success": true,
  "data": {
    "newLevel": 2,
    "levelTitle": "践行者",
    "unlockedPerks": [
      "可接"进阶"级任务",
      "匹配池扩大·更多客户可见你",
      "解锁"擅长标签"展示权"
    ]
  }
}
```

---

## 🎨 前端交互流程

### 流程图
```
1. 学生完成任务
   ↓
2. 调用 /check 检查晋级条件
   ↓
3. 如果 shouldLevelUp = true
   ↓ 显示通知卡（在成长主页）
4. 学生点击通知卡
   ↓
5. 调用 /dialog 获取对话内容
   ↓
6. 展示底部弹窗
   ├─ 导师讲述（mentorText）
   ├─ 数据卡片（dataCards）
   └─ 问题和选项
   ↓
7. 学生选择选项
   ↓
8. 调用 /answer 提交答案
   ↓
9. 300ms后：导师回复淡入（mentorReply）
   ↓
10. 700ms后：晋级按钮淡入
    ↓
11. 学生点击"走，升Lv.X"
    ↓
12. 调用 /confirm 确认晋级
    ↓
13. 跳转到晋级完成页
```

### 交互细节

**弹窗显示**：
- 从底部滑入，带弹性动画
- 背景半透明遮罩，带模糊效果
- 底部距离 TabBar 高度（80px）

**选项交互**：
- 点击选项：高亮边框变为橙红色
- 选项字母圆圈：背景变为橙红色，文字变白
- 同时只能选择一个
- 选择后300ms，导师回复出现在对话区底部
- 选择后700ms，关闭按钮和晋级按钮同时出现

**关闭按钮**：
- 初始状态：opacity: 0, pointer-events: none
- 选择后：淡入显示
- 点击后：返回成长主页（不执行晋级）

**对话区滚动**：
- sheet-body 可滚动
- 导师回复出现后，自动滚动到底部
- 问题区域和按钮区域固定不动

---

## 💾 前端数据模型

### DialogData 接口
```typescript
interface DialogData {
  mentorText: string[]           // 导师讲述（多段）
  question: string               // 问题
  options: Option[]              // 选项数组
  mentorReply: string            // 导师回复
  dataCards?: DataCard[]         // 数据卡片（可选）
  timeline?: TimelineNode[]      // 时间线（可选）
}

interface Option {
  letter: 'A' | 'B' | 'C' | 'D'
  text: string
}

interface DataCard {
  rows: DataRow[]
}

interface DataRow {
  label: string
  value: string
  highlight?: boolean   // true=绿色, false=灰色
}

interface TimelineNode {
  date: string
  label: string
  isNow?: boolean      // 当前节点高亮
}
```

---

## 🎨 样式规范

### 颜色系统
```css
--rust: #BC6446        /* 主色 */
--terra: #D88760       /* 渐变色 */
--golden: #F2CD78      /* 高亮色 */
--text-1: #2C2018      /* 主文字 */
--text-2: #6B5540      /* 次级文字 */
--text-3: #A8917A      /* 辅助文字 */
--border: #EDE5DC      /* 边框 */
--surface: #FFFFFF     /* 卡片背景 */
--surface2: #F5F0EB    /* 二级背景 */
```

### 导师对话块样式
```css
.mentor-block {
  background: linear-gradient(135deg, #2C1A10, #3D2415);
  border-radius: 24px;
  padding: 14px 16px;
  color: rgba(255, 255, 255, 0.88);
}
```

### 数据卡片样式
```css
.data-card {
  background: rgba(255, 255, 255, 0.07);
  border: 1px solid rgba(242, 205, 120, 0.18);
  border-radius: 16px;
  padding: 9px 12px;
}

.data-row {
  display: flex;
  justify-content: space-between;
  padding: 4px 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.06);
}

.data-row:last-child {
  border-bottom: none;
}

.dv.g { color: #6EE7A0; }  /* 绿色高亮 */
.dv.d { color: rgba(255, 255, 255, 0.38); }  /* 灰色 */
```

---

## 📝 前端实现清单

### 必须实现的页面/组件

1. **GrowthHome（成长主页）** ✅
   - [ ] 显示当前等级徽章
   - [ ] 显示进度条
   - [ ] 显示晋级通知卡
   - [ ] 通知卡点击触发弹窗

2. **LevelUpSheet（晋级弹窗组件）** 🆕
   - [ ] 底部弹窗动画
   - [ ] 导师信息栏
   - [ ] 可滚动的对话区域
   - [ ] 固定的问题区域
   - [ ] 选项按钮交互
   - [ ] 导师回复淡入
   - [ ] 晋级按钮淡入
   - [ ] 关闭按钮

3. **LevelUpDone（晋级完成页）** 🆕
   - [ ] 全屏黑色背景
   - [ ] 光晕呼吸动画
   - [ ] 粒子飘落效果
   - [ ] 等级数字展示
   - [ ] 解锁权益列表
   - [ ] 继续按钮

### 可复用的组件

**DataCard（数据卡片）**
```vue
<template>
  <view class="data-card">
    <view 
      v-for="(row, index) in rows" 
      :key="index" 
      class="data-row"
    >
      <text class="dk">{{ row.label }}</text>
      <text 
        class="dv" 
        :class="{ g: row.highlight, d: !row.highlight }"
      >
        {{ row.value }}
      </text>
    </view>
  </view>
</template>
```

**Timeline（时间线）**
```vue
<template>
  <view class="timeline">
    <view 
      v-for="(node, index) in nodes" 
      :key="index" 
      class="tl-item"
    >
      <view 
        class="tl-dot" 
        :class="{ now: node.isNow }"
      />
      <view class="tl-body">
        <text class="tl-date">{{ node.date }}</text>
        <text 
          class="tl-label" 
          :class="{ now: node.isNow }"
        >
          {{ node.label }}
        </text>
      </view>
    </view>
  </view>
</template>
```

---

## 🔧 调试建议

1. **使用mock数据测试**：
   - 先用静态数据测试UI和交互
   - 确保动画流畅
   - 再接入真实接口

2. **分阶段实现**：
   - 第一步：静态弹窗和样式
   - 第二步：选项交互
   - 第三步：导师回复动画
   - 第四步：接入真实数据

3. **测试不同等级**：
   - Lv.0→1：有数据卡片
   - Lv.1→2：有数据卡片
   - Lv.2→3：有数据卡片
   - Lv.3→4：有数据卡片
   - Lv.4→5：有时间线

---

## 📞 接口联调注意事项

1. **认证**：所有接口都需要在 header 中带 `Authorization: Bearer <token>`

2. **错误处理**：
   - 如果接口返回错误，显示友好提示
   - 不要让弹窗卡住

3. **数据完整性**：
   - 后端会基于学生真实数据生成内容
   - 如果数据不足，会使用默认值
   - 前端应该优雅处理空数据情况

4. **性能**：
   - `/dialog` 接口可能需要1-2秒处理
   - 建议加loading状态

---

## 🎯 最终效果

当学生完成第3单任务后：
1. 成长主页出现通知卡："导师想跟你说几句话"
2. 点击通知卡，底部弹出弹窗
3. 导师说："三单了。我翻了一下你第一单和这一单..."
4. 展示数据：第1单改了3次，第3单直接过
5. 问："你觉得，是什么不一样了？"
6. 学生选择选项
7. 导师回复："行。不管是哪种，三单都做完了。走，Lv.2。"
8. 点击晋级按钮
9. 进入仪式感十足的完成页
10. 显示解锁的新权益

---

**后端已完成，等待前端集成！** 🚀
