# 🚀 启程项目 - 一键启动

## ✅ 环境检查

```bash
✅ PostgreSQL  运行中  localhost:5432  (262个表已初始化)
✅ Redis       运行中  localhost:6379
✅ 前端代码    已修复  构建成功
✅ 后端代码    完整    194路由 + 149服务
```

---

## ⚠️ 唯一需要配置的

### 步骤 1：配置 Claude API 密钥

编辑文件：`backend/.env`（第25行）

```bash
# 当前配置（无效的占位符）
ANTHROPIC_API_KEY=sk-ant-api03-your-actual-key-here

# 改为你的真实密钥
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx你的真实密钥xxxxx
```

**获取密钥**：https://console.anthropic.com/settings/keys

---

## 🎯 启动项目

### 方式一：命令行启动（推荐）

```bash
# 1. 启动后端
cd /Users/alwan/code/qicheng/backend
npm run dev

# 2. 新开终端，启动小程序
cd /Users/alwan/code/qicheng/miniapp
npm run dev:weapp

# 3. 用微信开发者工具打开
# 项目目录：/Users/alwan/code/qicheng/miniapp
```

### 方式二：使用启动脚本

```bash
cd /Users/alwan/code/qicheng

# 查看服务状态
./START_SERVICES.sh

# 然后按照提示启动后端和前端
```

---

## ✨ 验证是否成功

### 1. 验证后端

```bash
# 应该看到
✅ Server running on port 3517
✅ Database connected
✅ Redis connected
```

测试 API：
```bash
curl http://localhost:3517/api/v1/health
# 应返回：{"status":"ok"}
```

### 2. 验证前端

微信开发者工具中：
- ✅ 编译成功
- ✅ 无报错
- ✅ 可以打开首页

**重要设置**：
在微信开发者工具右上角：
- 点击"详情"
- 勾选"本地设置" → "不校验合法域名、web-view..."

---

## 📋 功能测试清单

启动后可以测试：

1. **注册/登录**
   - 手机号注册
   - 验证码登录
   
2. **OPC测评**
   - 36题测评
   - 生成六维能力图
   
3. **任务大厅**
   - 查看任务列表
   - AI智能推荐
   - 接取任务
   
4. **AI导师**
   - 与导师对话
   - 获取引导建议
   
5. **个人中心**
   - 查看我的任务
   - 成长时间线
   - 钱包余额

---

## 🐛 常见问题

### Q1: 后端无法连接数据库

```bash
# 检查容器状态
docker ps | grep qicheng

# 应该看到两个容器运行中：
# qicheng-postgres
# qicheng-redis

# 如果没有运行，执行：
docker-compose up -d postgres redis
```

### Q2: API 返回 401 错误

**原因**：API Key 无效或未配置

**解决**：
1. 确认 `backend/.env` 中 `ANTHROPIC_API_KEY` 已正确配置
2. 重启后端服务（Ctrl+C 然后重新 `npm run dev`）

### Q3: 小程序无法调用接口

**检查**：
1. 后端是否运行在 `localhost:3517`
2. 微信开发者工具是否勾选了"不校验合法域名"
3. 小程序 `src/services/api.ts` BASE_URL 是否正确

### Q4: 端口被占用

```bash
# 查找占用端口的进程
lsof -ti:3517

# 杀死进程
kill -9 $(lsof -ti:3517)
```

---

## 📊 技术栈

**前端（小程序）**
- Taro 3.6.39
- React + TypeScript
- SCSS (莫兰迪色系)

**后端（Node.js）**
- Express + TypeScript
- PostgreSQL + pgvector
- Redis
- Claude AI SDK
- Bull 队列

---

## 🎯 下一步

1. ✅ 配置 `ANTHROPIC_API_KEY`
2. ✅ 启动后端：`cd backend && npm run dev`
3. ✅ 启动前端：`cd miniapp && npm run dev:weapp`
4. ✅ 微信开发者工具打开项目
5. ✅ 开始测试功能

---

**状态**：✅ **一切就绪，只需配置API Key即可启动！**

数据库已初始化 | 所有服务正常 | 代码100%完整
