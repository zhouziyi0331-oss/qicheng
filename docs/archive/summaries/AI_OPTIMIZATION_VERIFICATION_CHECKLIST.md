# 启程平台 - AI性能优化验收清单

**验收日期**: 待定  
**验收人**: 开发团队  
**优化版本**: P0+P1  
**参考文档**: AI_OPTIMIZATION_SUMMARY.md

---

## 📋 验收说明

本清单对应AI性能优化方案，逐项验证开发团队是否真正执行了优化。

**验收方式**：
- ✅ 已通过
- ⚠️ 部分通过
- ❌ 未通过
- ⏳ 待验证

---

## 一、硬优化验收

### 1.1 模型配置验证

#### 验证项1：默认模型是否为Claude Haiku

**验证方法**：
```sql
-- 检查模型分布
SELECT 
  model_name, 
  COUNT(*) as call_count,
  ROUND(COUNT(*)::numeric / SUM(COUNT(*)) OVER () * 100, 2) as percentage,
  AVG(latency_ms) as avg_latency
FROM ai_call_logs
WHERE created_at > NOW() - INTERVAL '24 hours'
GROUP BY model_name
ORDER BY call_count DESC;
```

**合格标准**：
- ✅ 90%以上为 `claude-haiku-4-5`
- ❌ 出现大量 `claude-sonnet-4-6` 或 `claude-3-5-sonnet-20241022` 记录

**验收结果**：⏳ 待验证

**实际数据**：
```
model_name              | call_count | percentage | avg_latency
------------------------|------------|------------|-------------
claude-haiku-4-5        |            |            |
claude-sonnet-4-6       |            |            |
```

---

#### 验证项2：maxTokens是否优化

**验证方法**：
```sql
-- 检查AI-06的output_tokens
SELECT 
  engine_name,
  AVG(output_tokens) as avg_output,
  MAX(output_tokens) as max_output,
  COUNT(*) as call_count
FROM ai_call_logs
WHERE engine_name LIKE 'AI-06%' 
  AND created_at > NOW() - INTERVAL '24 hours'
GROUP BY engine_name;
```

**合格标准**：
- ✅ AI-06平均output_tokens ≤ 400
- ✅ AI-06最大output_tokens ≤ 600
- ❌ 平均超过500或最大超过800

**验收结果**：⏳ 待验证

**实际数据**：
```
engine_name | avg_output | max_output | call_count
------------|------------|------------|------------
AI-06       |            |            |
```

---

### 1.2 流式输出验证

#### 验证项3：AI-06是否开启stream

**验证方法**：
1. 学生端打开导师对话窗口
2. 发送一条消息："这个任务我能做吗？"
3. 观察回复出现方式

**合格标准**：
- ✅ 1-2秒内开始出现第一个字（打字机效果）
- ✅ 文字逐字出现，不是一次性弹出
- ❌ 等待5秒以上才突然出现完整回复

**验收结果**：⏳ 待验证

**观察记录**：
- 首字延迟：___ 秒
- 显示方式：□ 逐字出现  □ 一次性弹出
- 总耗时：___ 秒

---

#### 验证项4：前端是否消费流式数据

**验证方法**：
1. 打开浏览器开发者工具
2. 切换到 Network → WS（WebSocket）标签
3. 发送导师对话消息
4. 观察WebSocket事件

**合格标准**：
- ✅ 看到多个连续的 `mentor_message_chunk` 事件
- ✅ 每个事件包含文字片段
- ✅ 最后一个事件标记完成
- ❌ 只有一个完整消息事件

**验收结果**：⏳ 待验证

**WebSocket事件记录**：
```
事件类型: mentor_message_chunk
事件数量: ___ 个
首个事件时间: ___ ms
最后事件时间: ___ ms
```

---

### 1.3 异步化覆盖度验证

#### 验证项5：AI-01画像生成异步化

**验证方法**：
1. 提交38题OPC测试
2. 观察提交后的页面表现

**合格标准**：
- ✅ 提交后立即显示"分析中"进度动画
- ✅ 页面不卡顿，可以返回或查看其他内容
- ❌ 提交后页面卡住，等待5秒以上才跳转

**验收结果**：⏳ 待验证

**观察记录**：
- 提交后响应时间：___ ms
- 是否显示进度动画：□ 是  □ 否
- 页面是否卡顿：□ 是  □ 否

---

#### 验证项6：AI-02项目匹配异步化

**验证方法**：
1. 进入推荐任务列表页面
2. 观察页面加载表现

**合格标准**：
- ✅ 列表区域立即出现骨架屏加载状态
- ✅ 不超过3秒显示结果
- ❌ 页面空白等待数秒

**验收结果**：⏳ 待验证

**观察记录**：
- 骨架屏出现时间：___ ms
- 结果显示时间：___ 秒
- 是否有空白等待：□ 是  □ 否

---

#### 验证项7：AI-03交付物审核异步化

**验证方法**：
1. 提交交付物
2. 观察提交后的状态

**合格标准**：
- ✅ 提交后立即显示"AI正在检查中"状态
- ✅ 有扫描动画或进度提示
- ❌ 点击提交按钮后卡死等待

**验收结果**：⏳ 待验证

**观察记录**：
- 提交后响应时间：___ ms
- 是否显示检查状态：□ 是  □ 否
- 是否有动画效果：□ 是  □ 否

---

### 1.4 Bull队列并发验证

#### 验证项8：队列并发数配置

**验证方法**：
1. 检查代码配置（`aiTaskQueue.ts`）
2. 或进行并发测试

**代码检查**：
```typescript
// 应该看到类似配置
aiTaskQueue.process(AITaskType.PROFILE_ANALYSIS, 3, async (job) => {...});
aiTaskQueue.process(AITaskType.PROJECT_CONDITION_ANALYSIS, 5, async (job) => {...});
aiTaskQueue.process(AITaskType.MATCH_ANALYSIS, 5, async (job) => {...});
aiTaskQueue.process(AITaskType.MENTOR_GUIDANCE, 2, async (job) => {...});
```

**并发测试**：
1. 两名学生同时提交38题测试
2. 记录两人从提交到画像生成完成的耗时

**合格标准**：
- ✅ 代码中配置了并发数（画像3、匹配5、导师2）
- ✅ 两人耗时接近（如都是3-5秒）
- ❌ 一个5秒一个15秒（说明串行处理）

**验收结果**：⏳ 待验证

**并发测试数据**：
```
学生A耗时：___ 秒
学生B耗时：___ 秒
耗时差异：___ 秒
```

---

### 1.5 上下文压缩验证

#### 验证项9：长对话上下文压缩

**验证方法**：
```sql
-- 找一个超过20轮对话的会话
SELECT 
  session_id,
  COUNT(*) as message_count
FROM mentor_messages
GROUP BY session_id
HAVING COUNT(*) > 20
ORDER BY message_count DESC
LIMIT 5;

-- 查看最新一次AI调用的input_tokens
SELECT 
  engine_name,
  input_tokens,
  output_tokens,
  latency_ms,
  created_at
FROM ai_call_logs
WHERE engine_name = 'AI-06'
  AND created_at > NOW() - INTERVAL '1 hour'
ORDER BY created_at DESC
LIMIT 10;
```

**合格标准**：
- ✅ 超过20轮对话后，input_tokens控制在1000-2000范围
- ✅ 延迟稳定，不随对话增长而增加
- ❌ input_tokens持续增长（如超过3000）

**验收结果**：⏳ 待验证

**实际数据**：
```
对话轮数：___ 轮
input_tokens：___
延迟：___ ms
```

---

### 1.6 pgvector索引验证

#### 验证项10：向量索引是否创建

**验证方法**：
```sql
-- 检查student_capabilities表的向量索引
SELECT 
  indexname, 
  indexdef
FROM pg_indexes
WHERE tablename = 'student_capabilities' 
  AND indexdef LIKE '%combined_vector%';

-- 检查索引类型
SELECT 
  indexname,
  indexdef
FROM pg_indexes
WHERE tablename = 'student_capabilities'
  AND indexdef LIKE '%ivfflat%';
```

**合格标准**：
- ✅ 存在 `idx_student_capabilities_vector` 索引
- ✅ 索引类型为 `ivfflat`
- ❌ 没有返回结果或索引类型不对

**验收结果**：⏳ 待验证

**实际数据**：
```sql
-- 索引查询结果
indexname: 
indexdef: 
```

---

### 1.7 Redis缓存验证

#### 验证项11：匹配结果缓存

**验证方法**：
1. 学生请求匹配推荐（第1次）
2. 立即再次请求匹配（第2次，下拉刷新）
3. 对比两次响应时间

**合格标准**：
- ✅ 第1次：3-5秒
- ✅ 第2次：<500ms（缓存命中）
- ❌ 第2次仍然等待数秒

**验收结果**：⏳ 待验证

**实际数据**：
```
第1次请求耗时：___ ms
第2次请求耗时：___ ms
缓存命中：□ 是  □ 否
```

---

#### 验证项12：Redis服务状态

**验证方法**：
```bash
# 检查Redis连接
redis-cli ping

# 查看缓存统计
curl http://localhost:3000/api/v1/admin/cache/stats
```

**合格标准**：
- ✅ Redis返回 PONG
- ✅ 缓存统计API返回正常数据
- ❌ Redis不可用或API报错

**验收结果**：⏳ 待验证

**实际数据**：
```json
{
  "enabled": true/false,
  "keyCount": ___,
  "memoryUsed": "___"
}
```

---

## 二、软优化（动画与加载状态）验收

### 2.1 AI-01画像生成等待页

#### 验证项13：画像生成动画

**验证方法**：
1. 提交38题测试
2. 观察等待页面

**合格标准**：
| 元素 | 要求 |
|------|------|
| 六维雷达图骨架 | ✅ 六条轴线依次亮起（动画） |
| 步骤进度说明 | ✅ 分步骤展示，当前步骤高亮 |
| 理念语句轮播 | ✅ 背景轮播品牌语句 |
| 取消按钮 | ✅ 超过5秒出现"稍后查看" |

**验收结果**：⏳ 待验证

**观察记录**：
- □ 雷达图动画
- □ 步骤进度
- □ 理念轮播
- □ 取消按钮

---

### 2.2 AI-03审核等待

#### 验证项14：审核动画

**验证方法**：
1. 提交交付物
2. 观察审核过程

**合格标准**：
| 元素 | 要求 |
|------|------|
| 扫描光效 | ✅ 交付物上有移动的光效 |
| 检查项轮播 | ✅ 轮播展示检查项 |
| 结果动画 | ✅ 通过/不通过有动画效果 |

**验收结果**：⏳ 待验证

**观察记录**：
- □ 扫描光效
- □ 检查项轮播
- □ 结果动画

---

### 2.3 万字报告生成等待

#### 验证项15：报告生成动画

**验证方法**：
1. 触发Lv.6毕业报告生成
2. 观察等待页面

**合格标准**：
| 元素 | 要求 |
|------|------|
| 章节目录骨架 | ✅ 六章目录依次亮起 |
| 章节名展示 | ✅ 显示当前生成章节名 |
| 内容轮播 | ✅ 轮播成长数据/工具推荐 |
| 通知选项 | ✅ 超过20秒提供通知选项 |

**验收结果**：⏳ 待验证

**观察记录**：
- □ 章节目录动画
- □ 章节名展示
- □ 内容轮播
- □ 通知选项

---

### 2.4 导师对话流式输出

#### 验证项16：打字机效果

**验证方法**：
1. 发送导师对话消息
2. 观察回复显示方式

**合格标准**：
| 元素 | 要求 |
|------|------|
| 闪烁光标 | ✅ 对话气泡有0.5秒间隔闪烁光标 |
| 打字机效果 | ✅ 文字逐字出现 |
| 中断恢复 | ✅ 中断超过3秒显示"正在思考" |

**验收结果**：⏳ 待验证

**观察记录**：
- □ 闪烁光标
- □ 打字机效果
- □ 中断恢复

---

### 2.5 通用加载规范验证

#### 验证项17：加载状态规范

**验证方法**：
测试各种操作的加载状态

**合格标准**：
| 规则 | 要求 |
|------|------|
| 200ms内反馈 | ✅ 按钮有按下态变化 |
| 超过2秒有进度 | ✅ 出现进度条或步骤指示器 |
| 超过5秒可取消 | ✅ 有取消按钮 |
| 超过30秒降级 | ✅ 自动降级为"稍后通知" |

**验收结果**：⏳ 待验证

**测试记录**：
- □ 200ms反馈
- □ 2秒进度
- □ 5秒取消
- □ 30秒降级

---

## 三、延迟数据对比验证

### 3.1 优化前后对比

#### 验证项18：延迟数据统计

**验证方法**：
```sql
-- 最近24小时的延迟统计
SELECT 
  engine_name,
  COUNT(*) as call_count,
  AVG(latency_ms) as avg_latency,
  PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY latency_ms) as p50_latency,
  PERCENTILE_CONT(0.95) WITHIN GROUP (ORDER BY latency_ms) as p95_latency,
  MAX(latency_ms) as max_latency
FROM ai_call_logs
WHERE created_at > NOW() - INTERVAL '24 hours'
GROUP BY engine_name
ORDER BY avg_latency DESC;
```

**合格标准**：
| 引擎/场景 | 优化前 | 优化后目标 | 实际 | 状态 |
|-----------|--------|-----------|------|------|
| AI-01 画像分析 | 15-25秒 | <8秒 | ___ | ⏳ |
| AI-02 匹配（缓存命中） | 5-10秒 | <500ms | ___ | ⏳ |
| AI-02 匹配（未命中） | 5-10秒 | <5秒 | ___ | ⏳ |
| AI-06 导师回复（首字） | 5-15秒 | <2秒 | ___ | ⏳ |
| AI-03 交付物审核 | 8-12秒 | <5秒 | ___ | ⏳ |
| AI-04 万字报告 | 25-40秒 | <20秒 | ___ | ⏳ |

**验收结果**：⏳ 待验证

---

## 四、破坏性测试

### 4.1 高峰期并发测试

#### 验证项19：并发处理能力

**验证方法**：
1. 3个学生同时提交38题测试
2. 记录每人的耗时

**合格标准**：
- ✅ 每人耗时都在5-8秒完成（并发正常）
- ❌ 每人耗时都超过20秒（串行处理）

**验收结果**：⏳ 待验证

**测试数据**：
```
学生A：___ 秒
学生B：___ 秒
学生C：___ 秒
平均：___ 秒
```

---

### 4.2 冷启动匹配测试

#### 验证项20：无缓存匹配性能

**验证方法**：
1. 用全新学生账号请求匹配推荐
2. 确保无Redis缓存
3. 记录响应时间

**合格标准**：
- ✅ 响应时间 <5秒
- ❌ 响应时间 >10秒

**验收结果**：⏳ 待验证

**测试数据**：
```
响应时间：___ 秒
```

---

### 4.3 长对话导师测试

#### 验证项21：长对话性能

**验证方法**：
1. 找一个有25轮历史对话的订单
2. 发送一条新消息
3. 记录回复时间

**合格标准**：
- ✅ 回复时间 <6秒
- ❌ 回复时间 >8秒（上下文压缩未生效）

**验收结果**：⏳ 待验证

**测试数据**：
```
对话轮数：___ 轮
回复时间：___ 秒
```

---

### 4.4 弱网环境测试

#### 验证项22：网络降级处理

**验证方法**：
1. 浏览器DevTools切换为Slow 3G网络
2. 执行各种操作
3. 观察页面表现

**合格标准**：
- ✅ 显示加载状态，不白屏
- ✅ 有超时提示和重试选项
- ❌ 页面直接白屏或报错

**验收结果**：⏳ 待验证

**观察记录**：
- □ 加载状态正常
- □ 超时提示
- □ 重试选项

---

### 4.5 流式中断测试

#### 验证项23：流式断点续传

**验证方法**：
1. 导师对话过程中临时断网
2. 恢复网络
3. 观察文字显示

**合格标准**：
- ✅ 恢复后继续显示，无跳帧或丢失
- ✅ 显示"正在思考"提示
- ❌ 文字跳帧或丢失

**验收结果**：⏳ 待验证

**观察记录**：
- □ 断点续传正常
- □ 提示信息
- □ 无内容丢失

---

## 五、最终验收标准

### 综合评分

| 维度 | 权重 | 合格标准 | 实际得分 | 状态 |
|------|------|---------|---------|------|
| **硬延迟** | 40% | 80%的AI调用<5秒，95%<10秒 | ___ | ⏳ |
| **感知延迟** | 30% | 所有>2秒等待有进度动画 | ___ | ⏳ |
| **流式输出** | 15% | AI-06 100%流式，首字<2秒 | ___ | ⏳ |
| **缓存命中** | 10% | 6小时内重复请求<500ms | ___ | ⏳ |
| **降级兜底** | 5% | >30秒自动降级 | ___ | ⏳ |

**总分**：___ / 100

**验收结论**：
- ✅ 通过（≥90分）
- ⚠️ 基本通过（70-89分）
- ❌ 不通过（<70分）

---

## 六、问题记录

### 发现的问题

| 序号 | 验证项 | 问题描述 | 严重程度 | 责任人 | 预计修复时间 |
|------|--------|---------|---------|--------|-------------|
| 1 | | | 🔴/🟡/🟢 | | |
| 2 | | | 🔴/🟡/🟢 | | |
| 3 | | | 🔴/🟡/🟢 | | |

### 优化建议

| 序号 | 建议内容 | 预期效果 | 优先级 |
|------|---------|---------|--------|
| 1 | | | P0/P1/P2 |
| 2 | | | P0/P1/P2 |
| 3 | | | P0/P1/P2 |

---

## 七、验收签字

| 角色 | 姓名 | 签字 | 日期 |
|------|------|------|------|
| 开发负责人 | | | |
| 测试负责人 | | | |
| 产品负责人 | | | |
| 技术负责人 | | | |

---

**验收清单版本**: v1.0  
**创建日期**: 2026-05-27  
**最后更新**: 2026-05-27

📋 **使用说明**：
1. 按顺序逐项验证
2. 记录实际数据和观察结果
3. 标记验收状态（✅/⚠️/❌/⏳）
4. 记录发现的问题
5. 最终评分和签字确认
