# 语义匹配核心修正：对齐匹配点

**修正日期**: 2026-05-26  
**问题**: 学生画像摘要和项目需求摘要结构不对应，导致无法在同一语义空间有效匹配  
**状态**: ✅ 已修正

---

## 问题诊断

### 之前的错误实现

**学生端**：生成的是自由格式的描述
```
"这个学生擅长从混乱的信息中提炼结构，习惯先搭框架再填细节。
在视觉表达上偏好简洁克制的风格，对文字排版有强迫症级别的在意。
上次给一家咖啡店做的菜单设计，客户说'这就是我想要但说不出来的感觉'。"
```

**项目端**：生成的是另一种格式的描述
```
"品牌视觉升级，需要图片/视频交付，预算5000-8000元，周期2周。
要求使用AI生图工具，不需要代码能力。"
```

**问题**：这两段文字在不同的语义层次上，一个描述"人的特质"，一个描述"项目要求"。Embedding模型无法找到深层语义关联。

---

## 核心原理

### 匹配点的本质

向量匹配不是标签撞库，而是**把学生和项目都翻译成同一种语言，在同一个空间里算距离**。

那个"同一种语言"就是1024维向量。但向量本身不是重点，**重点是生成向量的输入文本必须在同一个语义层次上对话**。

### 正确的做法

**学生端**：描述"我是怎样的人、我擅长做什么"  
**项目端**：描述"这个项目需要怎样的人、需要做成什么"

这两段话在语义上是**镜像的**。

---

## 修正后的实现

### 学生画像摘要模板（8个维度）

```
人格标签：[如"视觉叙事者"]
工作风格：[从信息处理维度提取，如"习惯先看全局再拆解，善于找到各部分之间的联系"]
创作偏好：[从创作驱动维度提取，如"灵感来源于视觉元素，擅长用画面讲故事"]
工具习惯：[从工具学习维度提取，如"拿到新工具直接上手试，边用边学"]
执行节奏：[从任务执行维度提取，如"习惯先出粗糙版本再一轮轮打磨"]
协作倾向：[从协作倾向维度提取，如"更喜欢自己从头到尾负责一个完整模块"]
风险偏好：[从风险态度维度提取，如"愿意尝试有挑战的新项目，但会先评估可行性"]
擅长方向：[基于六维综合判断，如"品牌视觉设计、社交媒体内容、创意广告"]
```

**完整示例**：
```
人格标签：视觉叙事者。
工作风格：习惯先看全局再拆解，善于找到各部分之间的联系。
创作偏好：灵感来源于视觉元素，擅长用画面讲故事。
工具习惯：拿到新工具直接上手试，边用边学。
执行节奏：习惯先出粗糙版本再一轮轮打磨。
协作倾向：更喜欢自己从头到尾负责一个完整模块。
风险偏好：愿意尝试有挑战的新项目，但会先评估可行性。
擅长方向：品牌视觉设计、社交媒体内容、创意广告。
```

### 项目需求摘要模板（8个维度，镜像对应）

```
需求类型：[从项目赛道和交付物类型提取，如"品牌视觉升级，需要图片/视频交付"]
工作风格匹配：[从项目特点提取，如"需要先从整体调性出发，再拆解到具体物料"]
创作偏好匹配：[从项目审美要求提取，如"需要强视觉表现力，能通过画面传递品牌情绪"]
工具习惯匹配：[从项目技术需求提取，如"使用AI生图工具即可，不需要写代码"]
执行节奏匹配：[从项目周期提取，如"周期2周，需要先出概念稿确认方向再细化"]
协作倾向匹配：[从项目管理方式提取，如"需求方给方向，具体执行由接单者独立完成"]
风险偏好匹配：[从项目确定性提取，如"方向明确，有参考案例，属于有挑战但不失控的项目"]
标签：[如"品牌视觉""社交媒体""创意广告"]
```

**完整示例**：
```
需求类型：品牌视觉升级，需要图片和视频交付。
工作风格匹配：需要先从整体品牌调性出发，再拆解到具体物料。
创作偏好匹配：需要强视觉表现力，能通过画面传递品牌情绪。
工具习惯匹配：使用AI生图工具即可完成，不需要代码能力。
执行节奏匹配：周期2周，建议先出概念稿确认方向，再细化。
协作倾向匹配：需求方给出品牌手册和方向，具体创意执行由接单者独立完成。
风险偏好匹配：方向明确，有参考案例，属于有挑战但不失控的项目。
标签：品牌视觉、社交媒体、创意广告。
```

---

## 为什么结构要对应？

因为Embedding模型（BGE）理解的是自然语言。

**对应的结构**：
- 学生："灵感来源于视觉元素，擅长用画面讲故事"
- 项目："需要强视觉表现力，能通过画面传递品牌情绪"
- **模型理解**：这两句话在语义上高度相关，都指向"视觉表达能力"

**不对应的结构**：
- 学生："视觉叙事者，Lv.3，完成5单"
- 项目："小红书图文，500元，2周交付"
- **模型理解**：只能做关键词匹配，找不到深层语义关联

---

## 完整流程

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 学生完成38题测试                                          │
│    ↓                                                         │
│    AI-01分析六维得分 + 人格标签 + 项目经历                   │
│    ↓                                                         │
│    生成结构化能力画像摘要（8个维度，约200字）                │
│    ↓                                                         │
│    调用BGE Embedding API                                     │
│    ↓                                                         │
│    得到1024维向量 → 存入student_capabilities.profile_vector  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 2. 企业发布项目                                              │
│    ↓                                                         │
│    AI分析项目标题 + 描述 + 技能要求 + 企业背景               │
│    ↓                                                         │
│    生成结构化需求摘要（8个维度，约200字，与学生画像镜像）    │
│    ↓                                                         │
│    调用BGE Embedding API（同一个模型）                       │
│    ↓                                                         │
│    得到1024维向量 → 存入tasks.requirement_vector             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 3. 匹配时                                                    │
│    ↓                                                         │
│    pgvector计算余弦相似度                                    │
│    SELECT 1 - (profile_vector <=> requirement_vector) AS similarity │
│    ↓                                                         │
│    返回相似度最高的学生                                      │
│    ↓                                                         │
│    AI-02读取两段原始摘要                                     │
│    ↓                                                         │
│    生成匹配理由："你擅长用画面讲故事，这个项目需要品牌视觉  │
│    升级——你的视觉叙事能力正好是它需要的。"                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 代码修改清单

### 1. vectorGenerationService.ts

**修改前**：
```typescript
// 生成自由格式的描述
const prompt = `生成一段200字左右的能力画像摘要...`;
```

**修改后**：
```typescript
// 生成结构化的8维度摘要
const prompt = `请严格按照以下结构生成摘要：
人格标签：...
工作风格：...
创作偏好：...
工具习惯：...
执行节奏：...
协作倾向：...
风险偏好：...
擅长方向：...`;
```

### 2. qichengTeacherService.ts

**新增方法**：
```typescript
async generateProjectRequirementSummary(taskId: string): Promise<string> {
  // 生成结构化的8维度项目需求摘要
  // 与学生画像摘要镜像对应
}
```

### 3. vectorGenerationService.ts - updateTaskEmbedding

**修改前**：
```typescript
// 使用原始标题+描述生成向量
const descriptionText = `${task.description}\n技能要求: ${task.required_skills}`;
const vector = await this.generateEmbedding(descriptionText, 1024);
```

**修改后**：
```typescript
// 使用结构化需求摘要生成向量
const requirementSummary = await qichengTeacherService.generateProjectRequirementSummary(taskId);
const vector = await this.generateEmbedding(requirementSummary, 1024);
```

### 4. vectorGenerationService.ts - updateStudentEmbedding

**修改前**：
```typescript
// 使用多个向量组合
const skillVector = await this.generateEmbedding(skillsText, 1536);
const trajectoryVector = await this.generateEmbedding(trajectoryText, 512);
// ... 组合多个向量
```

**修改后**：
```typescript
// 使用结构化画像摘要生成单一向量
const profileSummary = await this.generateStudentProfileSummary(studentId);
const vector = await this.generateEmbedding(profileSummary, 1024);
```

---

## 预期效果

### 匹配质量提升

| 场景 | 修正前 | 修正后 |
|------|--------|--------|
| 同义词识别 | ❌ "视频剪辑" vs "短视频制作" 不匹配 | ✅ 自动识别语义相关 |
| 深层语义 | ❌ "喜欢把乱的东西理清楚" vs "内部流程梳理" 不匹配 | ✅ 识别为高度相关 |
| 跨标签匹配 | ❌ 只能匹配相同标签 | ✅ 理解深层能力需求 |

### 具体案例

**学生画像**：
```
人格标签：视觉叙事者。
工作风格：习惯先看全局再拆解。
创作偏好：灵感来源于视觉元素，擅长用画面讲故事。
工具习惯：拿到新工具直接上手试。
执行节奏：习惯先出粗糙版本再打磨。
协作倾向：更喜欢独立负责完整模块。
风险偏好：愿意尝试有挑战的新项目。
擅长方向：品牌视觉设计、社交媒体内容。
```

**项目需求**：
```
需求类型：品牌视觉升级，需要图片/视频交付。
工作风格匹配：需要先从整体调性出发，再拆解到具体物料。
创作偏好匹配：需要强视觉表现力，能通过画面传递品牌情绪。
工具习惯匹配：使用AI生图工具即可。
执行节奏匹配：周期2周，先出概念稿再细化。
协作倾向匹配：需求方给方向，具体执行由接单者独立完成。
风险偏好匹配：方向明确，有参考案例。
标签：品牌视觉、社交媒体。
```

**匹配结果**：
- 余弦相似度：0.87（高度匹配）
- AI-02生成理由："你擅长用画面讲故事，这个项目需要品牌视觉升级——你的视觉叙事能力和独立执行风格正好是它需要的。"

---

## 关键要点

1. **匹配的不是标签，是两段在同一语义层次上对话的自然语言摘要**
2. **AI的工作是生成这两段结构对应的摘要**
3. **Embedding模型的工作是把它们转到同一个空间**
4. **pgvector的工作是量距离**
5. **结构必须对应，才能在同一语义空间有效匹配**

---

**修正完成**: ✅  
**下一步**: 配置API密钥后，重新生成所有学生和项目的向量  
**预期提升**: 匹配准确率从~30%提升到~70%+
