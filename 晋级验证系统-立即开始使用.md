# 🚀 晋级验证系统 - 立即开始使用

## ✅ 当前状态

- ✅ 后端代码已完成
- ✅ 前端代码已完成
- ✅ 小程序已成功编译
- ✅ 所有代码已推送到GitHub
- ✅ TypeScript错误已修复

**最新提交**: edd03315  
**编译状态**: ✅ 成功

---

## 📱 微信开发者工具运行

### 第1步：打开项目
1. 打开微信开发者工具
2. 导入项目
3. 项目路径：`/Users/alwan/code/qicheng/miniapp`
4. AppID：使用你的测试号或正式AppID

### 第2步：查看编译结果
- ✅ `dist/` 目录已生成
- ✅ `app.json` 配置正确
- ✅ 所有页面已编译

### 第3步：预览效果

#### 查看晋级完成页（可直接预览）
在开发者工具中访问：
```
pages/level-up-done/index
```

**测试参数**：
```
level=2
levelTitle=践行者
unlockedPerks=["可接\"进阶\"级任务","匹配池扩大","解锁\"擅长标签\""]
```

---

## 🔧 集成到主页

### 方案1：使用Hook（最简单）

在主页 `src/pages/index/index.tsx` 中：

```tsx
import { useLevelUp } from '../../hooks/useLevelUp'
import LevelUpNotificationCard from '../../components/LevelUpNotificationCard'
import LevelUpSheet from '../../components/LevelUpSheet'

export default function Index() {
  const levelUp = useLevelUp()

  useEffect(() => {
    // 页面加载时检查晋级
    levelUp.checkLevelUp()
  }, [])

  return (
    <View className="index">
      {/* 原有内容 */}
      
      {/* 晋级通知卡 */}
      {levelUp.showNotification && levelUp.checkData && (
        <LevelUpNotificationCard
          completedCount={levelUp.checkData.completedCount}
          onClick={levelUp.openLevelUpSheet}
        />
      )}

      {/* 晋级弹窗 */}
      {levelUp.dialogData && levelUp.checkData && (
        <LevelUpSheet
          visible={levelUp.showSheet}
          fromLevel={levelUp.checkData.fromLevel}
          toLevel={levelUp.checkData.toLevel}
          mentorText={levelUp.dialogData.mentorText}
          question={levelUp.dialogData.question}
          options={levelUp.dialogData.options}
          mentorReply={levelUp.dialogData.mentorReply}
          dataCards={levelUp.dialogData.dataCards}
          timeline={levelUp.dialogData.timeline}
          onClose={levelUp.closeSheet}
          onConfirm={levelUp.confirmLevelUp}
          onAnswer={levelUp.submitAnswer}
        />
      )}
    </View>
  )
}
```

### 方案2：Mock数据测试（不需要后端）

如果后端还没启动，可以用Mock数据先测试UI：

```tsx
import { useState } from 'react'
import LevelUpSheet from '../../components/LevelUpSheet'

export default function TestLevelUp() {
  const [visible, setVisible] = useState(true)

  const mockData = {
    mentorText: [
      '三单了。我翻了一下你第一单和这一单，有件事你应该知道。',
      '我不确定你自己有没有注意到这个变化——'
    ],
    question: '你觉得，是什么不一样了？',
    options: [
      { letter: 'A', text: '工具用熟了，不用想就知道点哪里' },
      { letter: 'B', text: '接到任务大概知道先做什么了' },
      { letter: 'C', text: '不那么怕做错了' },
      { letter: 'D', text: '我其实没觉得有什么不一样，就是做完了' }
    ],
    mentorReply: '行。不管是哪种，三单都做完了。\n\n走，Lv.2。',
    dataCards: [{
      rows: [
        { label: '第1单 · 修改次数', value: '3 次', highlight: false },
        { label: '第3单 · 修改次数', value: '0 次（直接过）', highlight: true },
        { label: '第1单 · 问导师次数', value: '5 次', highlight: false },
        { label: '第3单 · 问导师次数', value: '0 次（自己做完了）', highlight: true }
      ]
    }]
  }

  return (
    <LevelUpSheet
      visible={visible}
      fromLevel={1}
      toLevel={2}
      {...mockData}
      onClose={() => setVisible(false)}
      onConfirm={() => {
        console.log('确认晋级')
        Taro.navigateTo({ url: '/pages/level-up-done/index?level=2' })
      }}
      onAnswer={(option) => console.log('选择了', option)}
    />
  )
}
```

---

## 🗄️ 后端部署

### 第1步：初始化数据库

```bash
cd /Users/alwan/code/qicheng
./init-levelup-database.sh
```

**执行结果**：
- ✅ 创建 levelUpAnswers 集合
- ✅ 创建 levelUpHistory 集合
- ✅ 创建索引
- ✅ 更新现有数据

### 第2步：启动后端服务

```bash
cd miniapp/backend
npm run dev
```

**服务地址**: `http://localhost:3000`

### 第3步：测试API

```bash
cd /Users/alwan/code/qicheng
./test-level-up-api.sh
```

---

## 🎯 测试流程

### 测试1：查看晋级完成页
1. 在微信开发者工具中
2. 访问：`pages/level-up-done/index`
3. 添加编译模式，设置启动参数：
   ```
   level=2&levelTitle=践行者
   ```
4. 应该看到完整的动画效果

### 测试2：测试弹窗组件
1. 创建测试页面（使用上面的Mock数据）
2. 点击选项应该高亮
3. 300ms后导师回复淡入
4. 700ms后晋级按钮出现

### 测试3：完整流程（需要后端）
1. 启动后端服务
2. 在主页集成Hook
3. 完成任务后应该显示通知卡
4. 点击通知卡打开弹窗
5. 选择选项
6. 点击晋级
7. 跳转到完成页

---

## 📝 常见问题

### Q1: 小程序编译失败？
**A**: 确保 `dist/` 目录存在，运行：
```bash
cd miniapp
npm run build:weapp
```

### Q2: 组件样式不对？
**A**: 检查 `@import` 路径是否正确，确保导入了 `morandi-colors.scss`

### Q3: API调用失败？
**A**: 
1. 检查后端服务是否启动
2. 检查token是否存在
3. 查看控制台错误信息

### Q4: 动画不流畅？
**A**: 微信开发者工具的模拟器性能较差，在真机上测试效果更好

### Q5: 如何修改API地址？
**A**: 编辑 `src/services/levelUpAPI.ts`，修改 `BASE_URL` 常量

---

## 🎨 自定义配置

### 修改颜色主题
编辑 `src/styles/morandi-colors.scss`：
```scss
--rust: #BC6446;        // 主色
--terra: #D88760;       // 渐变色
--golden: #F2CD78;      // 高亮色
```

### 修改晋级所需任务数
编辑 `backend/src/services/levelUp.service.ts`：
```typescript
const LEVEL_REQUIREMENTS: Record<number, number> = {
  1: 1,   // Lv.0 → Lv.1
  2: 3,   // Lv.1 → Lv.2
  3: 5,   // Lv.2 → Lv.3
  4: 8,   // Lv.3 → Lv.4
  5: 10   // Lv.4 → Lv.5
}
```

### 修改等级名称
编辑 `backend/src/services/levelUp.service.ts`：
```typescript
const LEVEL_TITLES: Record<number, string> = {
  0: '新人',
  1: '初探者',
  2: '践行者',
  3: '专注者',
  4: '匠人',
  5: '导师'
}
```

---

## 📚 相关文档

- **前端集成文档**: `晋级验证系统-前端集成文档.md`
- **数据库设计**: `晋级验证系统-数据库设计.md`
- **集成示例**: `晋级验证系统-前端集成示例.md`
- **完整交付文档**: `晋级验证系统-完整交付文档.md`

---

## ✨ 快速命令

```bash
# 编译小程序
npm run build:weapp

# 开发模式（自动重编译）
npm run dev:weapp

# 初始化数据库
./init-levelup-database.sh

# 启动后端
cd miniapp/backend && npm run dev

# 测试API
./test-level-up-api.sh
```

---

## 🎉 开始使用

1. ✅ 小程序已编译成功
2. ✅ 组件已可用
3. ✅ 可以直接查看完成页效果
4. ⏳ 在主页集成Hook（参考上面的代码）
5. ⏳ 启动后端测试完整流程

**现在就可以在微信开发者工具中预览效果了！** 🚀

---

**最后更新**: 2026-07-24  
**编译状态**: ✅ 成功  
**Git提交**: edd03315
